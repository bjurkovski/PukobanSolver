 * codificar levels DONE
 * ler levels DONE
 * a* DONE
 * heurística de pegar os menores
   * implementar bfs no grafo implícito
	 * salvar numa matriz a menor distância de cada quadrado para um target
	 * atribuir isso para o h na função apply
 * pesquisa bibliográfica
 * determinar
   * número de movimentos
	 * branching factor (min, max, médio)
 * aprender a usar boost
   * download da 1_43_0
	 * ./bootstrap.sh
	 * sudo ./bjam install
 * colocar num grafo da boost
 * floyd warshall da boost
 * match

idéias novas:
 * calcular a heurística apenas se for ser inserido
 * tentar calcular incrementalmente
 * acessar as posições com caixa direto em vez de procurar em todo o board
 * estar ao mesmo tempo em todo o componente conexo

solução do 01:
 * 127 movimentos do pukoban
 * 50 movimentos de caixas
 * às vezes ele movimenta caixas sem necessidade, tem que tratar isso (execute a solução e verá)

